import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jinkojink.commandprotocols',
  appName: 'Command Protocols',
  webDir: 'dist'
};

export default config;
