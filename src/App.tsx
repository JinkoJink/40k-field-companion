import React,{useEffect,useMemo,useState} from 'react';
import {AlertTriangle,CheckCircle2,ChevronDown,Database,ExternalLink,Minus,Plus,RotateCcw,Search,Shield,Trash2,Settings} from 'lucide-react';
import {checkForUpdates,migrateLegacy,readBattle,readUser,system,writeBattle,writeUser} from './db';
import {createBattleState,phases,remainingUnitWounds,stateForRemainingWounds,syncBattleUnits,totalScore,totalUnitWounds,unitWounds} from './battle';
import {availableSizes,defaultSize,isCategory,loadNecrons,pointsFor,subfactionKeyword} from './data';
import {applyRequiredBindings,compatibleBodyguards,configurationGroups,createRosterUnit,defaultWargear,removeUnavailableEnhancements,rosterPoints,validateRoster} from './roster';
import {attachmentRoleFor,connectionNotesForEntry,eligibleEnhancementsForUnit,eligibleStratagemTargets,requiredBindingForUnit,retinueConditionFor,ruleMetaLine,stratagemAvailable} from './rules';
import type {BattleState,Detachment,Enhancement,Phase,RosterUnit,Stratagem,UnitDetail,UnitIndex,ValidationIssue} from './types';

const CORE_RULES_URL='https://www.warhammer-community.com/en-gb/downloads/warhammer-40000/';
const POINTS_LIMIT=2000;

export function App(){
  const[units,setUnits]=useState<UnitIndex[]>([]),[details,setDetails]=useState<Map<string,UnitDetail>>(new Map()),[detachments,setDetachments]=useState<Detachment[]>([]),[stratagems,setStratagems]=useState<Stratagem[]>([]),[selected,setSelected]=useState<string[]>(['Cursed Legion']),[roster,setRoster]=useState<RosterUnit[]>([]),[battle,setBattle]=useState<BattleState|null>(null),[tab,setTab]=useState<'build'|'battle'|'search'|'settings'>('build'),[query,setQuery]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true),[installed,setInstalled]=useState<any>(null),[updateMessage,setUpdateMessage]=useState(''),[settings,setSettings]=useState({automatic:true,wifiOnly:false,manualOnly:false});
  useEffect(()=>{Promise.all([migrateLegacy(),loadNecrons(),readUser<string[]>('detachments',['Cursed Legion']),readUser<RosterUnit[]>('roster',[]),readBattle(),readUser('settings',{automatic:true,wifiOnly:false,manualOnly:false})]).then(([,data,storedDetachments,storedRoster,storedBattle,storedSettings])=>{setUnits(data.index);setDetails(data.detailMap);setDetachments(data.detachments);setStratagems(data.stratagems);setSelected(storedDetachments);const activeEnhancements=data.detachments.filter(detachment=>storedDetachments.includes(detachment.name)).flatMap(detachment=>detachment.enhancements||[]);setRoster(applyRequiredBindings(storedRoster,data.index,activeEnhancements));setBattle(storedBattle);setSettings(storedSettings);return system('installed',null)}).then(setInstalled).catch(cause=>setError(String(cause))).finally(()=>setLoading(false));},[]);
  useEffect(()=>{if(!loading)void writeUser('roster',roster)},[roster,loading]);useEffect(()=>{if(!loading)void writeUser('detachments',selected)},[selected,loading]);useEffect(()=>{if(!loading)void writeBattle(battle)},[battle,loading]);useEffect(()=>{if(!loading)void writeUser('settings',settings)},[settings,loading]);useEffect(()=>{const connection=(navigator as Navigator&{connection?:{type?:string}}).connection,wifiBlocked=settings.wifiOnly&&(!connection||connection.type!=='wifi');if(!loading&&settings.automatic&&!settings.manualOnly&&navigator.onLine&&!battle&&!wifiBlocked){void checkForUpdates().then(async result=>{if(result.status==='updated'){setUpdateMessage(`Background update installed: ${result.changed.join(', ')}`);setInstalled(await system('installed',null));}}).catch(()=>{})}},[loading,settings.automatic,settings.manualOnly,settings.wifiOnly,battle]);useEffect(()=>{if(battle)setBattle(current=>current?syncBattleUnits(current,roster):current)},[roster.length]);
  const selectedDetachments=detachments.filter(detachment=>selected.includes(detachment.name)),selectedEnhancements=selectedDetachments.flatMap(detachment=>detachment.enhancements||[]),totalDP=selectedDetachments.reduce((sum,detachment)=>sum+detachment.dp,0),totalPoints=useMemo(()=>rosterPoints(roster,units,selectedDetachments),[roster,units,selectedDetachments]),issues=useMemo(()=>validateRoster({roster,units,details,detachments,selectedDetachments:selected,pointsLimit:POINTS_LIMIT}),[roster,units,details,detachments,selected]),errors=issues.filter(issue=>issue.level==='error'),filtered=units.filter(unit=>!unit.legends&&(!query||JSON.stringify({...unit,details:details.get(unit.id)}).toLowerCase().includes(query.toLowerCase())));
  function toggleDetachment(detachment:Detachment){if(selected.includes(detachment.name)){const nextSelected=selected.filter(name=>name!==detachment.name),nextDetachments=detachments.filter(candidate=>nextSelected.includes(candidate.name));setSelected(nextSelected);setRoster(current=>removeUnavailableEnhancements(current,nextDetachments));}else if(totalDP+detachment.dp<=3){const nextSelected=[...selected,detachment.name],nextDetachments=detachments.filter(candidate=>nextSelected.includes(candidate.name)),nextEnhancements=nextDetachments.flatMap(candidate=>candidate.enhancements||[]);setSelected(nextSelected);setRoster(current=>applyRequiredBindings(current,units,nextEnhancements));}}
  function addUnit(unit:UnitIndex){const created=createRosterUnit(unit,details.get(unit.id));setRoster(current=>applyRequiredBindings([...current,created],units,selectedEnhancements));}function patchEntry(instanceId:string,patch:Partial<RosterUnit>){setRoster(current=>current.map(entry=>entry.instanceId===instanceId?{...entry,...patch}:entry));}function changeSize(entry:RosterUnit,models:number){patchEntry(entry.instanceId,{models,wargear:defaultWargear(details.get(entry.unitId),models)});}function setWarlord(instanceId:string){setRoster(current=>current.map(entry=>({...entry,warlord:entry.instanceId===instanceId})))}
  return <main className='app'>
    <header className='top'><div><div className='eyebrow'>40K FIELD COMPANION</div><h1>Necrons — Strike Force</h1><p>Configure the army, validate it, then carry the same roster into battle.</p></div><div className={`points ${totalPoints>POINTS_LIMIT?'over':''}`}><b>{totalPoints}</b><span>/ {POINTS_LIMIT} pts</span></div></header>
    <div className='status'><span><Database size={14}/>{loading?'Loading rules data…':error?'Rules data unavailable':'Rules data ready'}</span><span>{totalDP}/3 DP selected</span><span className={errors.length?'invalid':'valid'}>{errors.length?<AlertTriangle size={14}/>:<CheckCircle2 size={14}/>} {errors.length?`${errors.length} legality issue${errors.length===1?'':'s'}`:'Army legal'}</span></div>{error&&<div className='error'>{error}</div>}
    <nav className='tabs'>{(['build','battle','search','settings'] as const).map(name=><button className={tab===name?'active':''} onClick={()=>setTab(name)} key={name}>{name[0].toUpperCase()+name.slice(1)}</button>)}</nav>
    {tab==='build'&&<BuildView units={units} details={details} detachments={detachments} selected={selected} totalDP={totalDP} roster={roster} issues={issues} onToggleDetachment={toggleDetachment} onAdd={addUnit} onPatch={patchEntry} onSize={changeSize} onWarlord={setWarlord} onRemove={instanceId=>setRoster(current=>current.filter(entry=>entry.instanceId!==instanceId))}/>} 
    {tab==='battle'&&<BattleView roster={roster} units={units} details={details} selectedDetachments={selectedDetachments} enhancements={selectedEnhancements} stratagems={stratagems} issues={errors} battle={battle} setBattle={setBattle}/>} 
    {tab==='search'&&<SearchView query={query} setQuery={setQuery} units={filtered} details={details} onAdd={addUnit}/>} 
    {tab==='settings'&&<SettingsView installed={installed} settings={settings} setSettings={setSettings} updateMessage={updateMessage} onCheck={async()=>{try{setUpdateMessage('Checking…');const result=await checkForUpdates(true);setUpdateMessage(result.status==='updated'?`Installed: ${result.changed.join(', ')}`:result.status==='current'?'Local rules are current.':'Offline — local rules remain active.');setInstalled(await system('installed',null));}catch(cause){setUpdateMessage(`Update rejected: ${String(cause)}`)}}}/>} 
  </main>;
}

function SettingsView({installed,settings,setSettings,updateMessage,onCheck}:{installed:any;settings:{automatic:boolean;wifiOnly:boolean;manualOnly:boolean};setSettings:React.Dispatch<React.SetStateAction<{automatic:boolean;wifiOnly:boolean;manualOnly:boolean}>>;updateMessage:string;onCheck:()=>Promise<void>}){const patch=(change:Partial<typeof settings>)=>setSettings(current=>({...current,...change}));return <section className='panel'><div className='row'><div><div className='eyebrow'>LOCAL RULES DATABASE</div><h2>Offline settings</h2></div><Settings size={22}/></div><p className='muted'>Rules, rosters and active battles live on this phone. Network checks only fetch a tiny manifest, then only changed packages.</p><div className='formGrid'><label className='checkLabel'><input type='checkbox' checked={settings.automatic} onChange={e=>patch({automatic:e.target.checked,manualOnly:!e.target.checked})}/> Automatic update checks</label><label className='checkLabel'><input type='checkbox' checked={settings.manualOnly} onChange={e=>patch({manualOnly:e.target.checked,automatic:!e.target.checked})}/> Manual updates only</label><label className='checkLabel'><input type='checkbox' checked={settings.wifiOnly} onChange={e=>patch({wifiOnly:e.target.checked})}/> Wi‑Fi only (automatic checks pause when detection is unavailable)</label></div><div className='rulePanel'><strong>Installed dataset: {installed?.datasetVersion||'initializing'}</strong><p>Schema {installed?.schemaVersion||1} · Last successful update: {installed?.lastSuccessfulUpdate?new Date(installed.lastSuccessfulUpdate).toLocaleString():'—'} · {navigator.onLine?'Online':'Offline'}</p></div><button className='addButton' onClick={()=>void onCheck()}>Check for updates now</button>{updateMessage&&<p className='muted'>{updateMessage}</p>}</section>}

function BuildView(props:{units:UnitIndex[];details:Map<string,UnitDetail>;detachments:Detachment[];selected:string[];totalDP:number;roster:RosterUnit[];issues:ValidationIssue[];onToggleDetachment:(detachment:Detachment)=>void;onAdd:(unit:UnitIndex)=>void;onPatch:(instanceId:string,patch:Partial<RosterUnit>)=>void;onSize:(entry:RosterUnit,models:number)=>void;onWarlord:(instanceId:string)=>void;onRemove:(instanceId:string)=>void;}){
  const{units,details,detachments,selected,totalDP,roster,issues}=props,[catalogueQuery,setCatalogueQuery]=useState(''),selectedDetachments=detachments.filter(detachment=>selected.includes(detachment.name)),enhancements=selectedDetachments.flatMap(detachment=>detachment.enhancements||[]),instanceIssues=(id:string)=>issues.filter(issue=>issue.unitInstanceId===id),catalogue=units.filter(unit=>!unit.legends&&(!catalogueQuery||JSON.stringify({...unit,details:details.get(unit.id)}).toLowerCase().includes(catalogueQuery.toLowerCase()))),categories=Array.from(catalogue.reduce((groups,unit)=>{const category=catalogueCategory(unit);groups.set(category,[...(groups.get(category)||[]),unit]);return groups},new Map<string,UnitIndex[]>()).entries()).sort(([a],[b])=>a.localeCompare(b));
  const connectionRows=roster.flatMap(entry=>{const unit=units.find(candidate=>candidate.id===entry.unitId);if(!unit)return[];const notes=connectionNotesForEntry(entry,unit,roster,units,enhancements);return notes.length?[{entry,unit,notes}]:[];});
  return <>
    <section className='panel'><div className='row'><div><div className='eyebrow'>DETACHMENTS</div><h2>{totalDP}/3 DP</h2></div><a href={CORE_RULES_URL} target='_blank' rel='noreferrer'>Core rules <ExternalLink size={14}/></a></div><p className='muted'>Select detachments while their combined cost remains at or below 3 DP. UNIQUE tags are enforced.</p><div className='detGrid'>{detachments.map(detachment=>{const picked=selected.includes(detachment.name),disabled=!picked&&totalDP+detachment.dp>3;return <button key={detachment.name} disabled={disabled} className={`det ${picked?'picked':''}`} onClick={()=>props.onToggleDetachment(detachment)}><div className='row'><strong>{detachment.name}</strong><span>{detachment.dp} DP</span></div><small>{detachment.objective}</small><p>{detachment.ruleText||detachment.summary}</p></button>})}</div></section>
    <section className='validation panel'><div className='eyebrow'>ARMY VALIDATION</div>{!issues.length?<p className='validLine'><CheckCircle2 size={16}/> Army passes every configured legality check.</p>:<ul>{issues.map((issue,index)=><li className={issue.level} key={index}>{issue.message}</li>)}</ul>}</section>
    {!!connectionRows.length&&<section className='panel'><div className='eyebrow'>CONDITIONAL RULE CONNECTIONS</div>{connectionRows.map(({entry,unit,notes})=><div className='rulePanel' key={entry.instanceId}><strong>{unit.name}</strong>{notes.map((note,index)=><p key={index}>{note}</p>)}</div>)}</section>}
    <section className='sectionHead'><div><h2>Your roster</h2><p>Each copy is configured separately, including unit size, loadout, Leader, Support, Retinue, Upgrade, Binding and Enhancement.</p></div></section>{!roster.length&&<div className='empty'>Add units from the catalogue below.</div>}
    <div className='stack'>{roster.map((entry,index)=>{const unit=units.find(candidate=>candidate.id===entry.unitId);if(!unit)return null;const detail=details.get(unit.id),bodyguards=compatibleBodyguards(entry,unit,roster,units,enhancements),availableEnhancements=eligibleEnhancementsForUnit(unit,enhancements),requiredBinding=requiredBindingForUnit(unit,enhancements),retinue=retinueConditionFor(unit),role=attachmentRoleFor(unit),attachmentLabel=retinue?.label||role,notes=connectionNotesForEntry(entry,unit,roster,units,enhancements);return <article className={`card rosterCard ${instanceIssues(entry.instanceId).length?'cardInvalid':''}`} key={entry.instanceId}><div className='row'><div><div className='eyebrow'>UNIT {index+1}{attachmentLabel?` · ${attachmentLabel.toUpperCase()}`:''}</div><h3>{unit.name}</h3></div><button className='iconButton danger' onClick={()=>props.onRemove(entry.instanceId)} aria-label={`Remove ${unit.name}`}><Trash2 size={17}/></button></div><div className='formGrid'><label>Unit size<select value={entry.models} onChange={event=>props.onSize(entry,Number(event.target.value))}>{availableSizes(unit).map(size=><option key={size} value={size}>{size} models</option>)}</select></label>{isCategory(unit,'character')&&<label className='checkLabel'><input type='radio' name='warlord' checked={Boolean(entry.warlord)} onChange={()=>props.onWarlord(entry.instanceId)}/> Warlord</label>}{(availableEnhancements.length>0||entry.enhancement)&&<EnhancementToggles name={`enhancement-${entry.instanceId}`} selected={entry.enhancement} enhancements={availableEnhancements} required={Boolean(requiredBinding)} onChange={enhancement=>props.onPatch(entry.instanceId,{enhancement})}/>} {attachmentLabel&&<label>{retinue?`${retinue.label} bodyguard (optional)`:role==='support'?'Support bodyguard (required)':'Leader bodyguard'}<select value={entry.attachedTo||''} onChange={event=>props.onPatch(entry.instanceId,{attachedTo:event.target.value||undefined})}><option value=''>{retinue||role==='leader'?'Not attached':'Select bodyguard'}</option>{bodyguards.map(target=>{const targetUnit=units.find(candidate=>candidate.id===target.unitId);return <option key={target.instanceId} value={target.instanceId}>{targetUnit?.name}</option>})}</select></label>}</div><WargearEditor entry={entry} detail={detail} onPatch={props.onPatch}/>{!!notes.length&&<div className='rulePanel'><strong>Active connections</strong>{notes.map((note,i)=><p key={i}>{note}</p>)}</div>}{!!instanceIssues(entry.instanceId).length&&<div className='inlineIssues'>{instanceIssues(entry.instanceId).map((issue,i)=><span key={i}>{issue.message}</span>)}</div>}<UnitDetails data={detail}/></article>})}</div>
    <section className='sectionHead'><div><h2>Necron unit catalogue</h2><p>Search, then drill into the unit category you want.</p></div></section><div className='search catalogueSearch'><Search size={18}/><input value={catalogueQuery} onChange={event=>setCatalogueQuery(event.target.value)} placeholder='Search this catalogue by unit, role, keyword, weapon…'/></div>{!categories.length&&<div className='empty'>No units match that search.</div>}<div className='catalogueGroups'>{categories.map(([category,group])=><details className='catalogueGroup' open={Boolean(catalogueQuery)} key={category}><summary><span>{category}</span><small>{group.length} unit{group.length===1?'':'s'}</small></summary><div className='grid'>{group.map(unit=><UnitCard unit={unit} detail={details.get(unit.id)} onAdd={()=>props.onAdd(unit)} key={unit.id}/>)}</div></details>)}</div>
  </>;
}
function catalogueCategory(unit:UnitIndex){if(isCategory(unit,'epic hero'))return'Epic Heroes';if(isCategory(unit,'character'))return'Characters';if(isCategory(unit,'battleline'))return'Battleline';if(isCategory(unit,'dedicated transport'))return'Dedicated Transports';if(isCategory(unit,'vehicle'))return'Vehicles';if(isCategory(unit,'monster'))return'Monsters';if(isCategory(unit,'mounted'))return'Mounted';if(isCategory(unit,'swarm'))return'Swarms';if(isCategory(unit,'infantry'))return'Infantry';return unit.role?`${unit.role[0].toUpperCase()}${unit.role.slice(1)} units`:'Other units';}
function WargearEditor({entry,detail,onPatch}:{entry:RosterUnit;detail?:UnitDetail;onPatch:(id:string,patch:Partial<RosterUnit>)=>void}){const{choices,modelGroups}=configurationGroups(detail);if(!choices.length&&!modelGroups.length)return null;const updateChoices=(groupId:string,optionId:string)=>onPatch(entry.instanceId,{wargear:{...entry.wargear,choices:{...entry.wargear.choices,[groupId]:optionId}}}),updateModel=(optionId:string,count:number)=>onPatch(entry.instanceId,{wargear:{...entry.wargear,modelCounts:{...entry.wargear.modelCounts,[optionId]:Math.max(0,count)}}});return <details className='config' open><summary>Wargear and model loadouts <ChevronDown size={15}/></summary><div className='formGrid'>{choices.map(group=><label key={group.id}>{group.name}<select value={entry.wargear.choices[group.id]||''} onChange={event=>updateChoices(group.id,event.target.value)}>{group.options.map(option=><option key={option.id} value={option.id}>{option.name}</option>)}</select></label>)}</div>{modelGroups.map(group=><div className='loadoutGroup' key={group.id}><strong>{group.name}</strong><span className='muted'>Assign all {entry.models} models</span>{group.options.map(option=><label key={option.id}><span>{option.name}</span><input type='number' min='0' max={entry.models} value={entry.wargear.modelCounts[option.id]||0} onChange={event=>updateModel(option.id,Number(event.target.value))}/></label>)}</div>)}</details>}
function enhancementDescription(enhancement:Enhancement){const groups=enhancement.keywordRestrictionGroups?.length?`Requires one of: ${enhancement.keywordRestrictionGroups.map(group=>group.join(' + ')).join(' OR ')}.`:'',restrictions=enhancement.allowedHosts?.length?`Only: ${enhancement.allowedHosts.join(', ')}.`:enhancement.keywordRestrictions?.length?`Requires ${enhancement.keywordRestrictions.filter(value=>value!=='DNU').join(' + ')}.`:'',exclusions=enhancement.exclusionKeywords?.length?`Excludes ${enhancement.exclusionKeywords.join(', ')}.`:'',grants=enhancement.grantKeywords?.length?`Grants ${enhancement.grantKeywords.join(' + ')}.`:'',attachments=enhancement.attachmentBodyguardIds?.length?`Adds conditional bodyguard options.`:'';return [enhancement.kind==='binding'?'NECRODERMAL BINDING.':enhancement.upgrade?'UPGRADE.':'ENHANCEMENT.',groups||restrictions,exclusions,grants,attachments,enhancement.description||''].filter(Boolean).join(' ')}
function EnhancementToggles({name,selected,enhancements,required,onChange}:{name:string;selected?:string;enhancements:Enhancement[]|undefined;required?:boolean;onChange:(enhancement?:string)=>void}){return <fieldset className='enhancementToggles'><legend>Enhancement / Upgrade / Binding</legend><label className='enhancementToggle'><input type='radio' name={name} checked={!selected} disabled={required} onChange={()=>onChange(undefined)}/><span><strong>None</strong><small>{required?'A mandatory Binding applies to this unit.':'No Enhancement, Upgrade or Binding assigned.'}</small></span></label>{(enhancements||[]).map(enhancement=><label className={`enhancementToggle ${selected===enhancement.name?'selected':''}`} key={enhancement.id||enhancement.name}><input type='radio' name={name} checked={selected===enhancement.name} onChange={()=>onChange(enhancement.name)}/><span><strong>{enhancement.name} <em>+{enhancement.points} pts</em></strong><small>{enhancementDescription(enhancement)}</small></span></label>)}</fieldset>}
function UnitCard({unit,detail,onAdd}:{unit:UnitIndex;detail?:UnitDetail;onAdd:()=>void}){const retinue=retinueConditionFor(unit),role=attachmentRoleFor(unit),label=retinue?.label||role;return <article className='card'><div className='row'><div><h3>{unit.name}</h3><p>{pointsFor(unit,defaultSize(unit))} pts{label?` · ${label.toUpperCase()}`:''}</p></div><button className='addButton' onClick={onAdd}><Plus size={15}/> Add</button></div><Stats unit={unit}/><KeywordChips unit={unit}/>{!!unit.attachTo?.length&&<div className='leader'>Can {role==='support'?'support':'lead'}: {unit.attachTo.join(', ')}</div>}{retinue&&<div className='leader'>{retinue.label}: requires attached {retinue.requiredAttachedKeywords.join(' + ')}</div>}{unit.transportCapacity?.capacity&&<div className='leader'>Transport capacity: {unit.transportCapacity.capacity}</div>}<details><summary>Datasheet details</summary><UnitDetails data={detail}/></details></article>}

function configuredLoadout(entry:RosterUnit,detail?:UnitDetail){const{choices,modelGroups}=configurationGroups(detail),parts:string[]=[];for(const group of choices){const id=entry.wargear.choices[group.id],option=group.options.find(candidate=>candidate.id===id);if(option)parts.push(option.name)}for(const group of modelGroups)for(const option of group.options){const count=entry.wargear.modelCounts[option.id]||0;if(count>0)parts.push(`${count}× ${option.name}`)}return parts;}
function BattleView({roster,units,details,selectedDetachments,enhancements,stratagems,issues,battle,setBattle}:{roster:RosterUnit[];units:UnitIndex[];details:Map<string,UnitDetail>;selectedDetachments:Detachment[];enhancements:Enhancement[]|undefined;stratagems:Stratagem[];issues:ValidationIssue[];battle:BattleState|null;setBattle:React.Dispatch<React.SetStateAction<BattleState|null>>;}){
  if(!battle)return <section className='panel battleStart'><Shield size={36}/><h2>Start Battle Mode</h2><p>Your army roster becomes a persistent tabletop tracker for Wounds, models, Reanimation Protocols, CP and Victory Points.</p>{!!issues.length&&<div className='error'>Resolve the {issues.length} army legality issue{issues.length===1?'':'s'} before starting.</div>}<button disabled={!roster.length||Boolean(issues.length)} onClick={()=>setBattle(createBattleState(roster))}>Start battle</button></section>;
  const patch=(change:Partial<BattleState>)=>setBattle(current=>current?{...current,...change}:current);
  return <><section className='battleDashboard panel'><div className='row'><div><div className='eyebrow'>BATTLE ROUND {battle.round}</div><h2>{totalScore(battle)} VP</h2></div><button className='dangerButton' onClick={()=>setBattle(null)}><RotateCcw size={15}/> Reset battle</button></div><div className='trackerGrid'><Counter label='Command points' value={battle.cp} onChange={cp=>patch({cp:Math.max(0,cp)})}/><label>Battle round<select value={battle.round} onChange={event=>patch({round:Number(event.target.value)})}>{[1,2,3,4,5].map(round=><option value={round} key={round}>Round {round}</option>)}</select></label></div><div className='phaseBar'>{phases.map(phase=><button className={battle.phase===phase?'active':''} onClick={()=>patch({phase})} key={phase}>{phase}</button>)}</div></section>
    <section className='panel'><div className='eyebrow'>ROUND SCORING</div><div className='scoreGrid'>{[1,2,3,4,5].map(round=><div className={battle.round===round?'currentRound':''} key={round}><strong>R{round}</strong><label>Primary<input type='number' min='0' value={battle.score[round]?.primary||0} onChange={event=>patch({score:{...battle.score,[round]:{...battle.score[round],primary:Number(event.target.value)}}})}/></label><label>Secondary<input type='number' min='0' value={battle.score[round]?.secondary||0} onChange={event=>patch({score:{...battle.score,[round]:{...battle.score[round],secondary:Number(event.target.value)}}})}/></label></div>)}</div></section>
    <StratagemPanel phase={battle.phase} stratagems={stratagems} selectedDetachments={selectedDetachments} roster={roster} units={units}/>
    <section className='panel'><div className='eyebrow'>ACTIVE DETACHMENT RULES</div>{selectedDetachments.map(detachment=><div className='rulePanel' key={detachment.name}><strong>{detachment.name} · {detachment.ruleName}</strong><p>{detachment.ruleText||detachment.summary||'No local rule text available.'}</p></div>)}</section>
    <div className='stack'>{roster.map(entry=>{const unit=units.find(candidate=>candidate.id===entry.unitId);if(!unit)return null;const state=battle.units[entry.instanceId]||{modelsRemaining:entry.models,woundsLost:0,destroyed:false},wounds=unitWounds(unit),maximumWounds=totalUnitWounds(entry.models,wounds),woundsRemaining=remainingUnitWounds(state,entry.models,wounds),modelsRemaining=Math.ceil(woundsRemaining/wounds),enhancement=(enhancements||[]).find(candidate=>candidate.name===entry.enhancement),detail=details.get(unit.id),loadout=configuredLoadout(entry,detail),notes=connectionNotesForEntry(entry,unit,roster,units,enhancements),update=(change:Partial<typeof state>)=>patch({units:{...battle.units,[entry.instanceId]:{...state,...change}}}),setRemaining=(value:number)=>update(stateForRemainingWounds(state,entry.models,wounds,value));return <details className={`card battleUnit ${state.destroyed?'destroyed':''}`} key={entry.instanceId} open><summary><strong>{unit.name}</strong> · {modelsRemaining}/{entry.models} models · {woundsRemaining}/{maximumWounds} Wounds</summary><div className='row'><div><h3>{unit.name}</h3><p>{modelsRemaining}/{entry.models} models · {wounds} Wound{wounds===1?'':'s'} each</p></div><label className='checkLabel'><input type='checkbox' checked={woundsRemaining===0} onChange={event=>setRemaining(event.target.checked?0:Math.min(wounds,maximumWounds))}/> Destroyed</label></div>{enhancement&&<div className='assignedEnhancement'><div className='eyebrow'>{enhancement.kind==='binding'?'NECRODERMAL BINDING':enhancement.kind==='upgrade'?'ASSIGNED UPGRADE':'ASSIGNED ENHANCEMENT'}</div><strong>{enhancement.name} <span>+{enhancement.points} pts</span></strong><p>{enhancementDescription(enhancement)}</p></div>}<Stats unit={unit}/>{!!notes.length&&<div className='rulePanel'><strong>Active rule connections</strong>{notes.map((note,i)=><p key={i}>{note}</p>)}</div>}{!!loadout.length&&<div className='rulePanel'><strong>Configured loadout</strong><p>{loadout.join(' · ')}</p></div>}<div className='trackerGrid'><Counter label={`Wounds remaining (${maximumWounds} Starting Strength)`} value={woundsRemaining} max={maximumWounds} onChange={setRemaining}/><div className='counterBox'><span>Models remaining</span><div><b>{modelsRemaining}</b><small>of {entry.models}</small></div></div></div><UnitDetails data={detail} phase={battle.phase}/><button className='reanimate compact' disabled={woundsRemaining>=maximumWounds} onClick={()=>setRemaining(woundsRemaining+wounds)}><Plus size={13}/> Reanimation Protocols: return 1 model</button></details>})}</div>
    <label className='notes panel'>Battle notes<textarea value={battle.notes} onChange={event=>patch({notes:event.target.value})} placeholder='Reserves, once-per-battle abilities, target priorities…'/></label></>;
}

function StratagemPanel({phase,stratagems,selectedDetachments,roster,units}:{phase:Phase;stratagems:Stratagem[];selectedDetachments:Detachment[];roster:RosterUnit[];units:UnitIndex[]}){const enhancements=selectedDetachments.flatMap(detachment=>detachment.enhancements||[]),available=stratagems.filter(stratagem=>stratagemAvailable(stratagem,phase,selectedDetachments,roster,units));return <section className='panel stratagemPanel'><div className='row'><div><div className='eyebrow'>AVAILABLE STRATAGEMS</div><h2>{phase[0].toUpperCase()+phase.slice(1)} phase</h2></div><span className='muted'>{available.length} available</span></div>{!available.length?<p className='muted'>No Stratagems match this phase, selected detachments and your roster’s eligible targets.</p>:<div className='stratagemList'>{available.map(stratagem=>{const targets=eligibleStratagemTargets(stratagem,roster,units,enhancements);return <details className='stratagem' key={stratagem.id}><summary><span>{stratagem.name}{stratagem.type?` · ${stratagem.type}`:''}</span><small>{Number.isFinite(stratagem.cp)?`${stratagem.cp} CP`:'CP varies'}</small></summary>{stratagem.playerTurn&&<p className='muted'>Turn: {stratagem.playerTurn}</p>}{stratagem.when&&<div className='rulePanel'><strong>WHEN</strong><p>{stratagem.when}</p></div>}{stratagem.target&&<div className='rulePanel'><strong>TARGET</strong><p>{stratagem.target}</p></div>}{stratagem.effect&&<div className='rulePanel'><strong>EFFECT</strong><p>{stratagem.effect}</p></div>}{!stratagem.when&&stratagem.timing&&<p>{stratagem.timing}</p>}{stratagem.description&&<p className='muted'>{stratagem.description}</p>}{!!targets.length&&<div className='synergy'><span>Eligible now: {Array.from(new Set(targets.map(({unit})=>unit.name))).join(', ')}</span></div>}</details>})}</div>}</section>}
function Counter({label,value,onChange,max=99}:{label:string;value:number;onChange:(value:number)=>void;max?:number}){return <div className='counterBox'><span>{label}</span><div><button onClick={()=>onChange(Math.max(0,value-1))}><Minus size={15}/></button><b>{value}</b><button onClick={()=>onChange(Math.min(max,value+1))}><Plus size={15}/></button></div></div>}
function SearchView({query,setQuery,units,details,onAdd}:{query:string;setQuery:(value:string)=>void;units:UnitIndex[];details:Map<string,UnitDetail>;onAdd:(unit:UnitIndex)=>void}){return <><div className='search'><Search size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder='Search units, keywords, roles, weapons…'/></div><div className='stack'>{units.map(unit=>{const label=retinueConditionFor(unit)?.label||attachmentRoleFor(unit);return <article className='card' key={unit.id}><div className='row'><div><h3>{unit.name}</h3><p>{pointsFor(unit)} pts · {unit.role||'unit'}{label?` · ${label}`:''}</p></div><button className='addButton' onClick={()=>onAdd(unit)}><Plus size={15}/> Add</button></div><UnitDetails data={details.get(unit.id)}/></article>})}</div></>}
function Stats({unit}:{unit:UnitIndex}){const keys:[string,string][]=[['M','M'],['T','T'],['Sv','Sv'],['W','W'],['Ld','LD'],['LD','LD'],['OC','OC'],['InSv','Inv']];const seen=new Set<string>();return <div className='stats'>{keys.map(([key,label])=>{if(!unit.stats[key]||seen.has(label))return null;seen.add(label);return <div key={key}><span>{label}</span><b>{unit.stats[key]}</b></div>})}</div>}
function KeywordChips({unit}:{unit:UnitIndex}){const factionKeywords=unit.categories.filter(category=>category.toLowerCase().startsWith('faction:')),keywords=unit.categories.filter(category=>!category.toLowerCase().startsWith('faction:')),subfaction=subfactionKeyword(unit);return <div className='keywordChips'>{subfaction?<p><span>SUBFACTION</span><b>{subfaction}</b></p>:!!factionKeywords.length&&<p><span>FACTION KEYWORDS</span>{factionKeywords.map(keyword=><b key={keyword}>{keyword.replace(/^Faction:\s*/i,'')}</b>)}</p>}{!!keywords.length&&<p><span>KEYWORDS</span>{keywords.map(keyword=><b key={keyword}>{keyword}</b>)}</p>}</div>}
function UnitDetails({data,phase}:{data?:UnitDetail;phase?:Phase}){if(!data)return <p className='muted'>Loading datasheet…</p>;const retinue=retinueConditionFor(data),role=attachmentRoleFor(data),weapons=(data.weapons||[]).filter(weapon=>!phase||(phase==='shooting'&&weapon.type==='Ranged Weapons')||(phase==='fight'&&weapon.type==='Melee Weapons')),abilities=(data.abilities||[]).filter(ability=>{if(retinue&&ability.name.toLowerCase()==='leader')return false;if(role==='support'&&ability.name.toLowerCase()==='leader')return false;if(!phase)return true;const structured=ability.rule?.phases||[ability.rule?.phase].filter(Boolean);if(structured.length)return structured.includes('any')||structured.includes(phase);const text=`${ability.name} ${Object.values(ability.characteristics).join(' ')}`.toLowerCase();return text.includes(phase)||phase==='command'||!/(movement|shooting|charge|fight) phase/.test(text)});return <div className='details'><KeywordChips unit={data}/>{retinue?<div className='ability'><strong>{retinue.label.toUpperCase()}</strong><p>May join a Bodyguard unit only when it already contains an attached {retinue.requiredAttachedKeywords.join(' + ')} model.{retinue.forbiddenCompanionNames?.length?` Cannot share that Bodyguard with ${retinue.forbiddenCompanionNames.join(', ')}.`:''}</p></div>:role&&<div className='ability'><strong>{role.toUpperCase()}</strong><p>{role==='support'?'This unit must be attached during army construction and can share its bodyguard with one Leader.':'This unit can attach as a Leader and can share its bodyguard with one Support.'}</p></div>}{data.transportCapacity?.capacity&&<div className='ability'><strong>TRANSPORT CAPACITY</strong><p>{data.transportCapacity.capacity}{data.transportCapacity.keywordRestrictions?.length?` · Requires ${data.transportCapacity.keywordRestrictions.join(' + ')}`:''}{data.transportCapacity.exclusionKeywords?.length?` · Excludes ${data.transportCapacity.exclusionKeywords.join(', ')}`:''}</p></div>}{abilities.map(ability=><div className='ability' key={ability.id||ability.name}><strong>{ability.name}</strong><p>{ability.description||Object.values(ability.characteristics).filter(Boolean).join(' ')}</p>{ability.rule&&<small className='muted'>{[ability.rule.behavior,ruleMetaLine(ability.rule.usage),ruleMetaLine(ability.rule.scope),ability.rule.quality].filter(Boolean).join(' · ')}</small>}</div>)}{weapons.map(weapon=><div className='weapon' key={weapon.id||weapon.name}><strong>{weapon.name}</strong><div className='weaponGrid'>{Object.entries(weapon.characteristics).map(([key,value])=><div key={key}><span>{key}</span><b>{value||'—'}</b></div>)}</div></div>)}</div>}
