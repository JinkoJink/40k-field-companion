# Android launcher asset generation

The source icon is `assets/icon.png`. Android launcher resources are generated with `@capacitor/assets@3.0.5` during `npm run build`, then validated before Gradle packaging.
